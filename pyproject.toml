[project]
name = "ghost-semantic-os"
version = "0.1.0"
description = "Text-only semantic computer-use runtime for a modified OSWorld environment"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "Ghost" }]

[project.urls]
Homepage = "https://github.com/zainmfjavaid/ghost-semantic-os"
Repository = "https://github.com/zainmfjavaid/ghost-semantic-os"
Issues = "https://github.com/zainmfjavaid/ghost-semantic-os/issues"

[tool.pyright]
pythonVersion = "3.10"
include = ["envserver", "guest_agent", "infra", "tests"]
