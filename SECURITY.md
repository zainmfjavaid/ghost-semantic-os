# Security

Please report vulnerabilities through GitHub's private security advisory
feature. Do not include live credentials, private task data, or exploitable
host details in a public issue.

This project is research infrastructure and is not a hardened multi-tenant
sandbox. Run it only on disposable or dedicated hosts and do not expose the
environment server to the public internet. See `docs/security-model.md`.
